{$DOMAIN_NAME} {
	# Respond hello world to all requests
	respond "Hello World"
}

routing.{$DOMAIN_NAME} {
	reverse_proxy 192.168.1.1:80
}

secrets.{$DOMAIN_NAME} {
	# Reverse proxy to bitwarden
	reverse_proxy localhost:10006
}
